# AI и Большие языковые модели (LLM)

**Дата создания:** 2025-12-13
**Последнее обновление:** 2025-12-14 ⚠️ ВАЖНЫЕ ИЗМЕНЕНИЯ
**Приоритет:** ⭐⭐⭐⭐⭐ (Критический — основа всего проекта)

---

## ⚠️ ОБНОВЛЕНО: Новые рекомендации (14 декабря 2025)

**После анализа GPT-5.2 и Gemini 3.0 рекомендации изменились!**

- 📖 **Детальное сравнение моделей:** [00-gpt-vs-gemini-comparison.md](00-gpt-vs-gemini-comparison.md)
- 📖 **Обоснование выбора:** [00-stack-rationale.md](00-stack-rationale.md)

**Коротко:**
- ✅ **Рекомендуется:** GPT-4o (стабильный, быстрый, проверенный)
- ⚠️ **НЕ рекомендуется для MVP:** GPT-5.2 (проблемы с производительностью и предсказуемостью)
- ❌ **НЕ использовать:** Gemini 3.0 (88% галлюцинации, нестабильность)

---

## Назначение

Большие языковые модели (LLM) — это "мозг" платформы. Они используются во ВСЕХ ИИ-агентах для анализа документов, генерации текста, принятия решений.

---

## Рекомендуемая связка

### Основной стек: OpenAI GPT-4o + LangChain

```
┌─────────────────────────────────────────┐
│         LangChain Framework             │
│  (оркестрация AI-агентов)               │
├─────────────────────────────────────────┤
│         OpenAI API (GPT-4o)             │
│  - gpt-4o для ВСЕХ задач (основная)     │
│  - gpt-4o-mini для простых задач        │
│                                         │
│  Fallback:                              │
│  - claude-3-5-sonnet (если OpenAI упал) │
└─────────────────────────────────────────┘
```

**Стоимость:** ~$1.50 на комплект ИД
**Скорость:** Быстро (в 2-3 раза быстрее GPT-5.2)
**Надёжность:** Высокая (проверен в продакшене)

---

## Конкретные инструменты

### 1. OpenAI GPT-4o (ОСНОВНАЯ МОДЕЛЬ)

**Что это:**
- Самая быстрая и эффективная модель GPT-4 от OpenAI
- Оптимальный баланс: скорость + качество + цена
- Используется в продакшене тысячами компаний

**Почему именно GPT-4o:**
- ✅ **Стабильный и предсказуемый** — одинаковые результаты всегда
- ✅ **Быстрый** — в 2-3 раза быстрее GPT-5.2
- ✅ **Дешевле** — $2.50/$10 за 1M токенов
- ✅ **Проверен временем** — нет неожиданных регрессий
- ✅ **Хорошо работает с грязными PDF** — реальные документы
- ✅ **Отличный русский язык**

**Рекомендуемые модели:**

| Модель | Использование | Стоимость (за 1M токенов) |
|--------|---------------|---------------------------|
| `gpt-4o` | **ВСЁ** (анализ РД, АОСР, поиск, проверка) | $2.5 input / $10 output |
| `gpt-4o-mini` | Простые тексты (экономия) | $0.15 input / $0.6 output |

**Установка:**
```bash
pip install openai
```

**Пример использования:**
```python
from openai import OpenAI

client = OpenAI(api_key="sk-...")

response = client.chat.completions.create(
    model="gpt-4-turbo",
    messages=[
        {"role": "system", "content": "Ты эксперт по анализу проектной документации в строительстве."},
        {"role": "user", "content": "Проанализируй эту спецификацию и определи необходимые АОСР..."}
    ],
    temperature=0.2  # Низкая температура для точности
)

print(response.choices[0].message.content)
```

**Официальный сайт:** https://platform.openai.com/docs/api-reference

---

### 2. LangChain (Фреймворк для AI-агентов)

**Что это:**
- Фреймворк для создания приложений на базе LLM
- Позволяет создавать цепочки действий (chains) и агентов
- Управляет памятью, промптами, интеграциями

**Почему именно LangChain:**
- ✅ Упрощает создание сложных AI-агентов
- ✅ Встроенная поддержка RAG (Retrieval-Augmented Generation)
- ✅ Легко переключаться между разными LLM
- ✅ Большая экосистема интеграций

**Установка:**
```bash
pip install langchain langchain-openai
```

**Пример создания агента:**
```python
from langchain_openai import ChatOpenAI
from langchain.prompts import ChatPromptTemplate
from langchain.chains import LLMChain

# Инициализация LLM
llm = ChatOpenAI(
    model="gpt-4-turbo",
    temperature=0.2,
    api_key="sk-..."
)

# Создание промпта
prompt = ChatPromptTemplate.from_messages([
    ("system", "Ты агент для анализа проектной документации."),
    ("human", "{input}")
])

# Создание цепочки
chain = LLMChain(llm=llm, prompt=prompt)

# Использование
result = chain.invoke({"input": "Проанализируй спецификацию..."})
print(result["text"])
```

**Официальный сайт:** https://python.langchain.com/docs/get_started/introduction

---

### 3. LangGraph (Для сложных агентных workflow)

**Что это:**
- Расширение LangChain для создания multi-agent систем
- Позволяет строить граф взаимодействия агентов

**Когда использовать:**
- Когда нужно, чтобы несколько агентов работали вместе
- Для сложных workflow с условными переходами

**Установка:**
```bash
pip install langgraph
```

**Пример использования:**
```python
from langgraph.graph import StateGraph
from typing import TypedDict

class State(TypedDict):
    document: str
    aosr_list: list
    quality_docs: list

# Определение графа
workflow = StateGraph(State)

# Добавление узлов (агентов)
workflow.add_node("analyze_rd", analyze_rd_agent)
workflow.add_node("generate_aosr", generate_aosr_agent)
workflow.add_node("search_quality_docs", search_quality_docs_agent)

# Определение связей
workflow.add_edge("analyze_rd", "generate_aosr")
workflow.add_edge("generate_aosr", "search_quality_docs")

# Компиляция
app = workflow.compile()

# Запуск
result = app.invoke({
    "document": "Проектная документация...",
    "aosr_list": [],
    "quality_docs": []
})
```

**Официальный сайт:** https://langchain-ai.github.io/langgraph/

---

## Альтернативные варианты

### Fallback: Anthropic Claude 3.5 Sonnet (РЕКОМЕНДУЕТСЯ)

**Что это:**
- Fallback на случай, если OpenAI недоступен
- Качество близко к GPT-4o
- Лучше работает с длинными документами (200K токенов контекста)

**Когда использовать:**
- ✅ Если OpenAI API недоступен
- ✅ Если документы очень большие (>100 страниц)
- ✅ Как резервный вариант для критичных задач

**Стоимость:** $3 input / $15 output за 1M токенов

```bash
pip install anthropic
```

```python
from anthropic import Anthropic

client = Anthropic(api_key="sk-ant-...")

message = client.messages.create(
    model="claude-3-5-sonnet-20241022",
    max_tokens=4096,
    messages=[
        {"role": "user", "content": "Проанализируй проектную документацию..."}
    ]
)

print(message.content[0].text)
```

**Официальный сайт:** https://docs.anthropic.com/

---

### Для локального развертывания (без API)

**Ollama + Llama 3.1 (70B):**
- Бесплатно
- Полный контроль над данными
- Требует мощный сервер (минимум 40GB VRAM)

```bash
# Установка Ollama
curl -fsSL https://ollama.com/install.sh | sh

# Загрузка модели
ollama pull llama3.1:70b

# Использование
ollama run llama3.1:70b "Проанализируй документацию..."
```

**Интеграция с LangChain:**
```python
from langchain_community.llms import Ollama

llm = Ollama(model="llama3.1:70b")
response = llm.invoke("Проанализируй...")
```

**Официальный сайт:** https://ollama.com/

---

### Российские аналоги (для импортозамещения)

#### 1. YandexGPT Pro

**Характеристики:**
- Качество сопоставимо с GPT-3.5 Turbo
- Полностью российская инфраструктура
- Поддержка русского языка "из коробки"

**Стоимость:**
- ~1.2 руб за 1K токенов (промпт)
- ~2.4 руб за 1K токенов (completion)

**Установка:**
```bash
pip install yandex-cloud-ml-sdk
```

**Пример использования:**
```python
from yandex_cloud_ml_sdk import YCloudML

sdk = YCloudML(folder_id="...", auth="<API-KEY>")

model = sdk.models.completions('yandexgpt')
result = model.configure(temperature=0.2).run("Проанализируй документацию...")

print(result.alternatives[0].text)
```

**Официальный сайт:** https://yandex.cloud/ru/services/yandexgpt

---

#### 2. GigaChat от Сбера

**Характеристики:**
- Качество на уровне GPT-3.5
- Российская разработка
- Хорошо понимает специфические российские термины

**Стоимость:**
- Бесплатная версия для тестирования (лимиты)
- Платная: ~1 руб за 1K токенов

**Установка:**
```bash
pip install gigachat
```

**Пример использования:**
```python
from gigachat import GigaChat

with GigaChat(credentials="...", verify_ssl_certs=False) as giga:
    response = giga.chat("Проанализируй проектную документацию...")
    print(response.choices[0].message.content)
```

**Официальный сайт:** https://developers.sber.ru/gigachat

---

## Архитектура использования в проекте

### MVP: Распределение моделей по агентам

```
┌─────────────────────────────────────────────────────┐
│         AI-агенты платформы (MVP)                   │
├─────────────────────────────────────────────────────┤
│                                                     │
│  [Агент анализа РД] ⭐⭐⭐⭐⭐                        │
│  └─ GPT-4o (быстро + качественно)                  │
│                                                     │
│  [Агент генерации АОСР] ⭐⭐⭐⭐                      │
│  └─ GPT-4o (оптимальный баланс)                    │
│                                                     │
│  [Агент поиска документов качества] ⭐⭐⭐            │
│  └─ GPT-4o                                          │
│                                                     │
│  [Агент генерации документов качества] ⭐⭐          │
│  └─ GPT-4o-mini (шаблонная задача, экономия)       │
│                                                     │
│  [Агент проверки комплектности] ⭐⭐⭐⭐⭐            │
│  └─ GPT-4o + ручная валидация (критично!)          │
│                                                     │
│  [Агент анализа сканов (OCR)] ⭐⭐⭐⭐                │
│  └─ GPT-4o Vision (работа с изображениями)         │
│                                                     │
│  [Fallback для всех агентов]                        │
│  └─ Claude 3.5 Sonnet (если OpenAI недоступен)     │
│                                                     │
└─────────────────────────────────────────────────────┘
```

### Production: С добавлением GPT-5.2 (после 6+ месяцев)

```
Основа → GPT-4o (всё, как в MVP)
Критичные задачи → GPT-5.2 Thinking (с валидацией!)
Fallback → Claude 3.5 Sonnet
```

**⚠️ GPT-5.2 использовать ТОЛЬКО:**
- После 6-12 месяцев тестирования
- С обязательной валидацией результатов
- С fallback на GPT-4o при странных результатах

---

## Оценка стоимости API

### Пример расчета для 1 проекта (комплект ИД)

**Вводные:**
- Анализ РД: ~50K токенов (промпт + ответ)
- Генерация 10 АОСР: ~100K токенов
- Поиск документов качества: ~30K токенов
- Генерация документов качества: ~20K токенов
- Проверка комплектности: ~40K токенов

**Итого:** ~240K токенов на проект

**Стоимость (при использовании GPT-4o):**
- Input: 120K токенов × $2.5 / 1M = $0.30
- Output: 120K токенов × $10 / 1M = $1.20
- **ИТОГО: ~$1.50 на комплект ИД**

**При 100 проектах в месяц:** $150/месяц на LLM API

---

## Хранение промптов

### Рекомендация: LangSmith (для мониторинга и улучшения)

**Что это:**
- Платформа от создателей LangChain
- Позволяет отслеживать все запросы к LLM
- Помогает улучшать промпты
- Дебаггинг цепочек агентов

**Установка:**
```bash
pip install langsmith
```

**Настройка:**
```python
import os
os.environ["LANGCHAIN_TRACING_V2"] = "true"
os.environ["LANGCHAIN_API_KEY"] = "ls__..."
os.environ["LANGCHAIN_PROJECT"] = "pto-platform"
```

**Официальный сайт:** https://smith.langchain.com/

---

## Рекомендации по использованию

### ✅ Лучшие практики

1. **Используйте разные модели для разных задач:**
   - Сложные задачи → GPT-4 Turbo
   - Массовые задачи → GPT-4o
   - Простые задачи → GPT-3.5 Turbo

2. **Низкая температура для точности:**
   - Temperature = 0.1-0.3 для генерации актов
   - Temperature = 0.5-0.7 для креативных задач

3. **Используйте system prompts:**
   - Всегда задавайте роль и контекст
   - Примеры хороших и плохих ответов

4. **Структурируйте вывод:**
   - Используйте JSON Schema для парсинга ответов
   - Функции OpenAI (function calling)

5. **Кэшируйте результаты:**
   - Не запрашивайте одно и то же дважды
   - Используйте Redis для кэширования промежуточных результатов

---

### ❌ Чего избегать

1. **НЕ отправляйте слишком большие документы за раз:**
   - Разбивайте на чанки
   - Используйте RAG (Retrieval-Augmented Generation)

2. **НЕ доверяйте LLM на 100%:**
   - Всегда добавляйте валидацию результатов
   - Критичные проверки дублируйте программно

3. **НЕ используйте одну модель для всего:**
   - Это дорого и неэффективно

---

## Следующие шаги

1. **Зарегистрироваться на OpenAI Platform:**
   - https://platform.openai.com/signup

2. **Получить API ключ:**
   - https://platform.openai.com/api-keys

3. **Установить лимиты расходов:**
   - Настроить billing limits в личном кабинете

4. **Начать с простого агента:**
   - Создать тестовый агент для генерации одного АОСР

5. **Подключить LangSmith для мониторинга:**
   - https://smith.langchain.com/

---

## Связь с другими компонентами

- **[OCR](02-ocr.md):** LLM используется для анализа распознанного текста
- **[Backend](03-backend.md):** API интеграция с LLM через Python
- **[Database](08-database.md):** Хранение промптов и истории запросов
- **[Cloud](07-cloud.md):** Хостинг агентов в облаке

---

**Статус:** ✅ Готово к использованию
**Последнее обновление:** 2025-12-13
